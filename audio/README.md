# 音频说明

本项目音频全部使用本地相对路径加载，不依赖网络。

- `rain.wav`：第0幕、第2幕、第6-9幕，作为环境层循环播放，雨声峰值略低。
- `7lx.wav`：第1幕、第5幕、END1-END5。
- `music.wav`：封面 first 页、第3-4幕、第10-14幕、第16-22幕。
- `miao.wav`：第2幕、第15幕，播放完后间隔 1.5 秒再次播放。

按钮点击音不额外加载文件，由 `js/audio.js` 使用 Web Audio 生成很轻的点击声，减少体积。
