# 音频替换说明

当前版本只保留音频开关与调用接口，不加载真实音频文件。

后续如需加入离线音效，可在本目录放入体积较小的本地文件，并在 `js/audio.js` 中接入：

- `click.wav`：按钮点击
- `confirm.wav`：选项确认
- `transition.wav`：场景切换
- `secret.wav`：隐藏事件触发

所有文件都应保持本地相对路径引用，单个音效建议控制在几十 KB 内。
